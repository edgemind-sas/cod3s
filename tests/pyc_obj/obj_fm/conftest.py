"""Shared fixtures for ``tests/pyc_obj/obj_fm/``.

The PyCATSHOO singleton-release fixture now lives one level up
(``tests/pyc_obj/conftest.py``) so every ``pyc_obj`` test directory
(obj_fm, obj_deg_mode, fm_wiring, ...) gets it without duplication.
"""
